<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Adnode Front</title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag "#{FrontComponentDist.where(name: 'adnode-front-antd').limit(1).pluck(:file_url).join('')}" %>
    <%= stylesheet_link_tag "#{FrontComponentDist.where(name: 'adnode-front-app').limit(1).pluck(:file_url).join('')}" %>
    <%= javascript_include_tag "#{FrontComponentDist.where(name: 'adnode-front-js').limit(1).pluck(:file_url).join('')}" %>
  </head>
  <body>
    <div id="app"></div>
  </body>
</html>

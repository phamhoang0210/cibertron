<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Adnode Front</title>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="app"></div>
    <% @dists.each do |dist| %>
      <%= javascript_include_tag dist.file_url if dist.is_js? %>
      <%= stylesheet_link_tag dist.file_url if dist.is_css? %>
    <% end %>
    <script type="text/javascript">function add_chatinline(){var hccid=95813407;var nt=document.createElement("script");nt.async=true;nt.src="https://mylivechat.com/chatinline.aspx?hccid="+hccid;var ct=document.getElementsByTagName("script")[0];ct.parentNode.insertBefore(nt,ct);}
  add_chatinline();</script>
  </body>
  
</html>

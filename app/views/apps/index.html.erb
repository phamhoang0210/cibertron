<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Adnode Front</title>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <div id="app"></div>
    <% @dists.each do |dist| %>
      <%= javascript_include_tag dist.file_url if dist.is_js? %>
      <%= stylesheet_link_tag dist.file_url if dist.is_css? %>
    <% end %>
  </body>
</html>
